# ГЛАВНАЯ СТРАНИЦА
"https://stellarburgers.education-services.ru/"

(By.XPATH, ".//button[text()='Оформить заказ']") # КНОПКА ОФОРМИТЬ ЗАКАЗ
# КНОПКИ ВХОДА
(By.XPATH, ".//button[text()='Войти в аккаунт']")  # Кнопка «Войти в аккаунт» на главной
(By.XPATH, ".//p[text()='Личный Кабинет']")  # Кнопка «Личный кабинет»
 

# ФОРМА РЕГИСТРАЦИИ
(By.NAME, 'name')  # Поле «Имя» в форме регистрации
(By.XPATH, ".//fieldset[2]//input[@name = 'name']")  # Поле Email в форме регистрации
(By.NAME, 'Пароль')  # Поле пароля в форме регистрации
(By.XPATH, ".//button[text()='Зарегистрироваться']")  # Кнопка регистрации
(By.XPATH, ".//p[text()='Некорректный пароль']")  # Сообщение об ошибке для пароля
(By.XPATH, ".//a[text()='Войти']")  # Кнопка входа в форме регистрации

# ФОРМА ВХОДА
(By.NAME, 'name')  # Поле email в форме входа
(By.NAME, 'Пароль')  # Поле пароля в форме входа
(By.XPATH, ".//button[text()='Войти']")  # Кнопка входа в форме
(By.XPATH, ".//a[text()='Восстановить пароль']")  # Кнопка восстановления пароля

# ЛИЧНЫЙ КАБИНЕТ
(By.XPATH, ".//button[text()='Выход']")  # Кнопка выхода из аккаунта

# КОНСТРУКТОР
(By.XPATH, ".//p[text()='Конструктор']")  # Кнопка «Конструктор»
(By.XPATH, ".//a[@href = '/']")  # Логотип Stellar Burgers
(By.XPATH, ".//span[text()='Булки']")  # Кнопка Булки  
(By.XPATH, ".//h2[text()='Булки']") # Текст Булки внутри конструктора для проверки
(By.XPATH, ".//span[text()='Соусы']")  # Кнопка Соусы  
(By.XPATH, ".//h2[text()='Соусы']") # Текст Соусы внутри конструктора для проверки
(By.XPATH, ".//span[text()='Начинки']")  # Кнопка Начинки  
(By.XPATH, ".//h2[text()='Начинки']") # Текст Начинки внутри конструктора для проверки